query Reviews($episode: Episode, $first: Int, $offset: Int) {
  reviews(episode: $episode, first: $first, offset: $offset) {
    id
    episode
    stars
    commentary
    createdAt
    seenOn
  }
}
