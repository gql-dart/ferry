query Songs($first: Int!, $offset: Int!) {
  Song(first: $first, offset: $offset) {
    id
    __typename
    name
  }
}

mutation CreateSong($id: ID!, $name: String!, $fileName: String!) {
  CreateSong(id: $id, name: $name, fileName: $fileName) {
    id
  }
}
