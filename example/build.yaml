targets:
  $default:
    builders:
      gql_build|schema_builder:
        enabled: true
      gql_build|ast_builder:
        enabled: true
      gql_build|op_builder:
        enabled: true
        options:
          schema: ferry_example|lib/schema.graphql
      gql_build|data_builder:
        enabled: true
        options:
          schema: ferry_example|lib/schema.graphql
      gql_build|var_builder:
        enabled: true
        options:
          schema: ferry_example|lib/schema.graphql

      ferry|req_builder:
        enabled: true
