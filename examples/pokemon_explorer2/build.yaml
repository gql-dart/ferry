targets:
  $default:
    sources:
      - $package$
      - lib/**
    builders:
      ferry_generator2|graphql_builder:
        options:
          schema:
            file: pokemon_explorer2|lib/graphql/schema.graphql
            add_typenames: true
          scalars:
            jsonb:
              type: 'Map<String, dynamic>'
